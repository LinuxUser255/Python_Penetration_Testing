#!/usr/bin/env python3

#Backdoor --- (Rough draft)
import socket
import os
import platform
import glob, re


#Get sys info & the contents of a remote folder
for msg in glob.glob('/tmp/'.txt'):
        filer = open((msg),'r')
        data = filer.read()
        message = re.findall(r' message (. ?) /message ', data,re.DOTALL)
        print "File %s conmtains %s % (str(msg),message)
        filer.close()

